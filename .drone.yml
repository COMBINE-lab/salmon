 pipeline:
  setup:
   image: hbb:salmon_build
   commands:
    - echo "Starting build"
    - ./.drone/build.sh
  test_indexing:
   image: hbb:salmon_build
   commands:
    - echo "[Testing quant]"
    - ./.drone/test_quant.sh 
   volumes:
    - /mnt/scratch6/avi/data:/mnt/data
  copy_build:
    image: hbb:salmon_build
    commands:
     - echo "[Packaging binary]"
     - ./.drone/copy_build.sh
    volumes:
     - /mnt/scratch6/avi/data:/mnt/data
  notify_gitter:
    image: plugins/gitter
