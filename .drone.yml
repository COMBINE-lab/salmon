pipeline:
  build:
    image: ubuntu_16.04:salmon_build
    commands:
      - echo `pwd`
    script:
      - ./salmon/.drone/build.sh
    notify:
      email:
        recipients:
          - combinelab.sbu@gmail.com
    
